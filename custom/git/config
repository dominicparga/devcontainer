[include]
    path = "~/.gitconfig.general"
[core]
    autocrlf = input
    editor = vim
[user]
    name = Fabian Franzelin (CC/AD-ESB4)
    email = fabian.franzelin@de.bosch.com
[credential]
    helper = "cache --timeout=3600"  # cache credentials for 60 minutes
[http "https://athena.*.corpintra.net"]
    username = franzef
    sslCert = ~/.local/share/certificates/cert-dap.crt
    sslKey = ~/.local/share/certificates/cert-dap.key
    sslCaInfo = /etc/ssl/certs/ca-certificates.crt
[http "https://*.daimler.com"]
    username = franzef
    sslCert = ~/.local/share/certificates/cert-dap.crt
    sslKey = ~/.local/share/certificates/cert-dap.key
    sslCaInfo = /etc/ssl/certs/ca-certificates.crt
[credential "https://scm-manager.rd.corpintra.net"]
    username = franzef
    sslCert = ~/.local/share/certificates/cert-dap.crt
    sslKey = ~/.local/share/certificates/cert-dap.key
    sslCaInfo = /etc/ssl/certs/ca-certificates.crt
[credential "https://github.com"]
    username = fabianfranzelin
[merge]
    tool = meld
[diff]
    tool = visual
[difftool "visual"]
    cmd = ${VISUAL} --wait --diff $LOCAL $REMOTE
[filter "lfs"]
    clean = git-lfs clean -- %f
    smudge = git-lfs smudge -- %f
    process = git-lfs filter-process
    required = true
